CREATE TABLE regions (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL
);

CREATE TABLE postings (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL
);

CREATE TABLE ranks (
    code TEXT PRIMARY KEY,
    name TEXT NOT NULL
);